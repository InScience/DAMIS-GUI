{% block body %}
    {% for parameter in parameters %}
        {% if parameter.connectionType.type == 'OUTPUT_CONNECTION'
            or parameter.connectionType.type == 'INPUT_CONNECTION' %}
            {% include "@DamisExperiment/_experiment_connection.html.twig"
                with {parameter : parameter} %}
        {% else %}
            {% include "@DamisExperiment/_experiment_component.html.twig"
            with {parameter : parameter} %}
        {% endif %}
    {% endfor %}
{% endblock %}
