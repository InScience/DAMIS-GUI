{% extends "FOSUserBundle::layout.html.twig" %}

{% block body -%}
    <h1>{{ 'layout.users'|trans({}, 'FOSUserBundle') }}</h1>

    {{ grid(grid, "BaseMainBundle:Grid:blocks.html.twig") }}
{% endblock %}